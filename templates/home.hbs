{{>header}}
<div class="content">
    <section class="js">
        <h2>JavaScript</h2>
        <div class="articles">
            {{#if javaScript}}
            {{#each javaScript}}
            <article>
                <h3>{{title}}</h3>
                <p>{{content}}</p>
                <a href="/edit/{{@key}}" class="btn details-btn">Details</a>
            </article>
            {{/each}}
            {{else}}
                <h3 class="no-articles">No articles yet</h3>
            {{/if}}
        </div>
    </section>
    <section class="CSharp">
        <h2>C#</h2>
        <div class="articles">
           {{#if cSharp}}
            {{#each cSharp}}
            <article>
                <h3>{{title}}</h3>
                <p>{{content}}</p>
                <a href="/edit/{{@key}}" class="btn details-btn">Details</a>
            </article>
            {{/each}}
            {{else}}
                <h3 class="no-articles">No articles yet</h3>
            {{/if}}
    </section>
    <section class="Java">
        <h2>Java</h2>
        <div class="articles">
               {{#if java}}
            {{#each java}}
            <article>
                <h3>{{title}}</h3>
                <p>{{content}}</p>
                <a href="/edit/{{@key}}" class="btn details-btn">Details</a>
            </article>
            {{/each}}
            {{else}}
                <h3 class="no-articles">No articles yet</h3>
            {{/if}}
        </div>
    </section>
    <section class="Pyton">
        <h2>Pyton</h2>
        <div class="articles">
            {{#if pyton}}
            {{#each pyton}}
            <article>
                <h3>{{title}}</h3>
                <p>{{content}}</p>
                <a href="/edit/{{@key}}" class="btn details-btn">Details</a>
            </article>
            {{/each}}
            {{else}}
                <h3 class="no-articles">No articles yet</h3>
            {{/if}}
        </div>
    </section>
</div>
{{>footer}}